<#if package?? && package != "">
package ${package};
</#if>

import bundles.models.input.panels.ModelMixPanel;
import java.awt.Component;

/**
 *
 * @author ${user}
 */
public class ${name}InputMIXView extends ModelMixPanel {

    public ${name}InputMIXView(${name} model) {
        super(new ${name}InputView(model), new Component[]{
            new ${name}DetailView(),
            new ${name}DetailView()});
    }

}
