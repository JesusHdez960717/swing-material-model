<#if package?? && package != "">
package ${package};
</#if>

import bundles.models.input.dialogs.DialogInputCBS;
import components.comboboxes.icbs.validados.ICBSNotEmptySeleccionable;
import java.awt.event.ActionListener;

/**
 *
 * @author ${user}
 */
public class ${name}ICBS extends ICBSNotEmptySeleccionable<${name}> {

    public ColorICBS() {
        super("${name}");
    }

    @Override
    public void actualizarComboBox() {
        getComboBox().removeAllItems();
        getComboBox().addItem("Seleccione...");
        for (${name} obj : new ${name}Service().find${name}Entities()) {
            getComboBox().addItem(obj);
        }
    }

    @Override
    public ActionListener buttonAddAction() {
        return new java.awt.event.ActionListener() {
            @Override
            public void actionPerformed(java.awt.event.ActionEvent evt) {
                onButtonAddActionPerformed();
            }
        };
    }

    private void onButtonAddActionPerformed() {
        new DialogInputCBS(this, new ${name}InputView());
    }

}

